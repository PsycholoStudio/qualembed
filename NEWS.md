# qualembed 1.0.1

Citation metadata only; the code is identical to 1.0.0.

* `citation("qualembed")` now carries DOIs: Zenodo's concept DOI for the
  software, and the preprint's DOI for the paper. The paper was posted after
  1.0.0 was archived, so neither was available then.
* README shows what `citation()` returns, rather than only the call.

# qualembed 1.0.0

First version.
